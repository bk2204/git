[package]
name = "git-internal"
version = "0.1.0"
edition = "2021"
rust-version = "1.63.0"

[lib]
crate-type = ["staticlib"]

[dependencies]
formatx = "0.2"
gettext-rs = { version = "0.7", optional = true }

[features]
gettext = ["dep:gettext-rs"]
